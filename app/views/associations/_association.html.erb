<span class="ingredient_image">
	<%= image_tag association.ingredient.image, :title => association.ingredient.name, :class => "ingredient handle" %>
</span>
<span class="ingredient_amount">
	<%= association.amount %>
</span>
