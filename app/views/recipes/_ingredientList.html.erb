
<div class="ingredient_list">
	
	<h3>ingredients</h3>
	<hr />
	<% @recipe.associations.ingredient_list.each do |a| %>
		<div class="row">
			<%= "#{a.amount} #{a.ingredient.name}" %>
		</div>
	<% end %>
</div>